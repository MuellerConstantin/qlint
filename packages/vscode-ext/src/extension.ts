/**
 * Entry point invoked by the VS Code extension host once the extension is
 * activated. The current build only contributes the `qlik` language
 * association declaratively (see `contributes.languages` in package.json), so
 * there is no runtime wiring yet — this is a placeholder until the linting
 * integration lands.
 */
export function activate(): void {}

/**
 * Invoked by the VS Code extension host when the extension is deactivated.
 * Nothing is registered yet, so there is nothing to clean up here.
 */
export function deactivate(): void {}
