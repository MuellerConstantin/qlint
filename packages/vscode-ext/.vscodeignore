# Sources and build config — only the bundled `dist/` output, the `images/`
# assets (icon + README logo), and the README ship in the VSIX.
src/**
node_modules/**
tsconfig.json
tsdown.config.ts
.vscodeignore
